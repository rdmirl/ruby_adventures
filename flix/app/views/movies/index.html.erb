<h1><%= @movies.count %> Movies Found</h1>

The time is now: <%= Time.now %>

<ul>
  
	<% @movies.each do |movie| %>

		<li>
			<%= movie.title %>, 
			<%= movie.rating %>, 
			<%= number_to_currency(movie.total_gross) %>
		</li>

	<% end %>

</ul>